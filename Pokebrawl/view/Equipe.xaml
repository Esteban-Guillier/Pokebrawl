<Page x:Class="Pokebrawl.view.Equipe"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pokebrawl.view"
        mc:Ignorable="d"
        Title="Equipe" Height="450" Width="800">
    <Grid Background="White">
        <StackPanel Orientation="Vertical" Margin="20" HorizontalAlignment="Center">
            <TextBlock Text="Votre Équipe" FontSize="28" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <ItemsControl x:Name="PokemonList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl ItemsSource="{Binding Equipe}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Image Source="{Binding ImageFace}" Width="100" Height="100"/>
                                <TextBlock Text="{Binding Nom}" />
                                <TextBlock Text="{Binding Niveau}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ItemsControl>

            <Button Content="Retour au menu" Width="150" Margin="20" Click="Retour_Click"/>
        </StackPanel>
    </Grid>
</Page>
